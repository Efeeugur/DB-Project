# Art School Management System Configuration

# Application Settings
app.name=Art School Management System
app.version=1.0.0

# Window Settings
window.width=1200
window.height=800

# Database Settings (for future PostgreSQL integration)
db.host=localhost
db.port=5432
db.name=artschool
db.user=postgres
db.password=postgres

# Logging
log.level=INFO
log.file=app.log

# Default Values
default.course.capacity=20
default.skill.level=BEGINNER
